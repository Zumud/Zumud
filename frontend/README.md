# Zumud frontend

Next.js 16 (App Router) + React 19 + Tailwind 4. Talks to the FastAPI backend
on `:8000` (see the repo-root README and `AGENTS.md` for full local setup).

```bash
npm ci
npm run dev        # http://localhost:3000
npm run lint       # eslint
npm run typecheck  # tsc --noEmit
```
